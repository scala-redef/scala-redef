package redef

/**
 * Package containing utility classes and methods for the redef library.
 *
 *   - `Result`: A type for error handling, representing either a successful
 *     value or an error.
 *   - `Try`: A type for handling computations that may fail, encapsulating
 *     success or failure.
 *   - `Using`: A utility for managing resources with automatic cleanup.
 */
package object util {}
